# Nummirock WWW Prototype

Static HTML/CSS/JS prototype for the Nummirock website overhaul.

## Local preview

```sh
cd prototype
python3 -m http.server 4174
```

Open `http://localhost:4174/index.html`.

## GitHub Pages

The site deploys from the `prototype/` directory using GitHub Actions.

